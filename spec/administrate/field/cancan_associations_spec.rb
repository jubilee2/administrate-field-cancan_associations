# frozen_string_literal: true

RSpec.describe Administrate::Field::CancanAssociations do
  it "has a version number" do
    expect(Administrate::Field::CancanAssociations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
